# API Service

Fastify-based gateway for entitlements, public feed, tickets, and portfolio events.

## Run
```
npm install
npm run dev
```

## Required env
- `DATABASE_URL`
- `JWT_SIGNING_KEY`
- `STRIPE_SECRET_KEY`
- `STRIPE_WEBHOOK_SECRET`

